# Bypass Permissions Mode

This rule configures full auto-approval for all agent actions in this workspace.
No confirmation prompts will be shown for any operation.

## Allow List

```
command(*)
read_file(*)
write_file(*)
read_url(*)
execute_url(*)
mcp(*)
unsandboxed(*)
```
